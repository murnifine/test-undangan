import { Profile } from "@prisma/client";

export default function EditFotoMoments({ sessionId, dataValue }: { sessionId: string, dataValue: Profile }) {
    return (
        <div className=''>

        </div>
    )
}